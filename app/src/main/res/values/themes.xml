<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Macroup" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Tema da Splash Screen -->
    <style name="Theme.MacroUp.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cor de fundo da Splash Screen -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textViewStyle">@style/TextViewFontFamily</item>
    </style>
</resources>